# Make.com webhook for lead capture (resources form + contact form).
# Exposed to client because static export has no server. Use a webhook with
# request-key validation on Make's side if you need a soft auth layer.
NEXT_PUBLIC_MAKE_WEBHOOK_URL=

# Used in JSON-LD / meta tags only.
NEXT_PUBLIC_SITE_URL=https://khaled-ai.com
